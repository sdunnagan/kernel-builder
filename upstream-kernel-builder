#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)

# shellcheck source=lib/kernel-builder-common.sh
source "$SCRIPT_DIR/lib/kernel-builder-common.sh"

# shellcheck source=lib/kernel-builder-upstream.sh
source "$SCRIPT_DIR/lib/kernel-builder-upstream.sh"

upstream_main "$@"
